{
    "name": "draw-steel",
    "version": "0.0.1",
    "private": true,
    "packageManager": "yarn@4.0.0",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "devDependencies": {
        "turbo": "^1.12.0"
    },
    "scripts": {
        "dev": "turbo run dev --parallel",
        "build": "turbo run build",
        "lint": "turbo run lint"
    }
}
